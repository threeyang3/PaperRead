# Privacy and security

See [Security](security.md). Public build is blocked by user fields, personal
note markers, absolute paths, likely credentials, databases, logs, cache,
sessions, and PDFs. Remote Feed paths are resolved inside the Feed root and
cannot traverse to local files. Feed content is never executed.
