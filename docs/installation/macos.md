# macOS installation

Install Python 3.11+ and use `pipx install paperflow`, `uv tool install
paperflow`, or `scripts/install.sh`. Initialize with a Vault path under the
user's Obsidian directory. All configured Workspace roots remain relative.
