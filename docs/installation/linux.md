# Linux installation

Install Python 3.11+ and use `pipx install paperflow`, `uv tool install
paperflow`, or `scripts/install.sh`. Initialize an existing Obsidian Vault with
`paperflow init --vault /path/to/Vault`.

The PaperFlow Automation Obsidian plugin requires Obsidian Desktop to be open;
it does not rely on system cron.
