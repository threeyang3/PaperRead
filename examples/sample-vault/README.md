# Sample Vault fixture

This directory is intentionally free of real user papers. Tests create
synthetic Raw, AI, User, and Derived records in temporary directories so source
and release archives cannot accidentally contain the current user's library.
